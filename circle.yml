machine:
  php:
    version: 7.0.4
dependencies:
  cache_directories:
    - vendor
    - ~/.composer/cache
  override:
    - composer install --no-scripts --no-progress
test:
  pre:
    - mkdir -p $CIRCLE_TEST_REPORTS/phpunit
  override:
    - ./vendor/bin/grumphp run
  post:
    - cp junit.xml $CIRCLE_TEST_REPORTS/phpunit/junit.xml
